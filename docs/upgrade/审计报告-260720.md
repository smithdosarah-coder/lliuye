# 6Agent 产品审计报告（升级方案证据基座）260720

> 取证方式：7 路并行深读（92 万 token，全带文件:行号）+ 主会话亲验 5 处关键代码 + 生产环境 liuye.me 实测（admin 账号登录，11 页走查 + 报告全流程交互，截图 25 件在 `audit-shots-260720/`）+ 生产 API 探针。
> 证据锚定：行号基于 main HEAD `ad11cc4`（2026-06-05）；引用同时给符号名，行号漂移以符号为准（R1-16 采纳）。
> 分级：🔴 违背产品第一性（诚信线）｜🟡 结构债｜🟢 保留资产。
> 新增实锤（R1 亲验补强）：export 契约允许客户端覆盖权威 QC——`ExportDocxRequest` 含 qc 字段，`agent_report/api.py:1393-1394` `if req.qc is not None: payload["qc"] = req.qc`（:1809-1810 同型第二处）——旧前端或恶意调用可传 `qc={"passed":true}` 导出任意正文。

---

## A. 产品逻辑 · 诚信线（🔴 全部主会话亲验）

| # | 发现 | 证据 |
|---|---|---|
| A1 | **QC 全旁路**：blocker 逐段只打日志（"soft per-section log"）；QC FAIL 照落 docx 并返回下载链；export_docx 不查 qc.passed | section_generator.py:983；v16_runner.py:470-473；agent_report/api.py:1356-1415 |
| A2 | **缺数造词**：`_DEMO_SAFE_FALLBACK` **42 键字典 + 1 条 KB 未命中通用回退**（「（详见补充材料）」）——缺失字段填「本企业/成立多年/符合本行准入/（详见证照）」；注释自认"防客户经理向审贷员演示时露 backend 缺陷""不输出未能自动填写字面"；同时使 blocker 的占位残留检测失效（键数经 R1 纠偏后 AST 重数确认 42） | v16_op_handlers.py:946-999（fallback 注入点 :948）；kb_lookup_fill :209-214 |
| A3 | **合规判断层被预埋覆盖**：场景模式清空真实检出（ledger.normal.clear() / observation.clear()）后重放 scenario.json 预埋清单——真实规则引擎的非严重检出被丢弃 | agent_compliance/agent.py:314-325 |
| A4 | **证据不落盘**：EvidenceItem/EvidenceBundle 协议定义完备但主管线不消费；Phase1 证据文本是一次性 prompt 字符串用完即弃；无凭证坐标、无计算谱系；顶层 ledger_service 与报告链零接线；evidence_pipeline.py 自述"不调 LLM"是观测壳 | shared/evidence/protocol.py:29-55；agent_report/evidence_pipeline.py:48-55 |
| A5 | **财务一致性维恒空转**：五维 blocker 的 financial_consistency 需要 anchor dict，生产调用传 None | section_generator.py:987 |
| A6 | **授信输入造数**：申请额度缺失时硬编默认 300 万进入决策测算；员工数靠正则抠正文 | agent_credit/agent.py:114（`or 300`）、:108 |

**生产实锤（liuye.me 实测）**：恢复会话状态行「鼎盛商贸 · (待提取) · QC 阻断 · 待补材料 · 字段覆盖 100%」下，「导出 Word」按钮 enabled=true 且可点击（shots2/01b-after-dp001.png）；点击后返回 **HTTP 422 契约错误**且原始 FastAPI JSON 直接显示在错误横幅（shots2/05-after-export-click.png）——UI 层旁路与前后端契约漂移（pending_questions 形状不匹配）两个独立缺陷并存。

## B. 产品逻辑 · 判断与数据

| # | 级 | 发现 | 证据 |
|---|---|---|---|
| B1 | 🟢 | 判断层确定性方向正确：credit 四维评分纯插值、红线阈值比较；alert 22 规则零 LLM 2 秒百户；compliance 硬检查器默认关 LLM；channel 打分纯 Python Jaccard | scoring_model_corporate.py:77-198；cross_matcher.py:16；matrix_matcher.py:82-262,293-295；lookalike_matcher.py:4-8 |
| B2 | 🟡 | 红线单期绝对阈值：无行业分位数 B 轨、无多期趋势 | 红线 JSON schema；交叉比对:119 |
| B3 | 🟡 | verifier 无否决权：Phase3 LLM 审 LLM，corrected>原 50% 即采纳；数字守卫 errs2<errs 即接受（可带残错出货） | section_generator.py:884-896,913 |
| B4 | 🟡 | 数字校验覆盖窄：numeric_validator 白名单 ~25 tag，未命中静默跳过；「证据链完整性」检查=正文出现"根据/来源"任一关键词即过 | numeric_validator.py:335-339；quality_blocker.py:156-173 |
| B5 | 🟡 | 模板/客户硬编码：交行 8 表格假定、doc.tables[0].rows[3] 固定版式、客户名关键词写死 | material_anchor.py:10-12；template_decomposer.py:771-773 |
| B6 | 🟡 | 数据源最弱：mock + Tavily 文本搜；enterprise_info 靠 LLM 抽字段；全仓无 MCP 基础设施 | shared/sources/impls/；agent_channel/__init__.py:9 |
| B7 | 🟡 | 获客双管线并存：agent.py（SearchProvider 抽象）与 realtime_stream.py（Tavily 直连 1387 行）两套，生产走后者 | agent_channel/api.py:13；realtime_stream.py:399-434 |
| B8 | 🟡 | 双 QC 体系口径分裂：quality_scorer（75 分制 9 维）与 quality_blocker（五维）并存，无单一 verdict | 两文件并存 |

## C. harness（NFR 五件套逐项）

| 项 | 判定 | 证据 |
|---|---|---|
| 超时 | ❌ 无 | llm.py:69 未传 timeout（SDK 默认 600s） |
| 重试 | 半 | chat_json max_retries=2；无指数退避；靠 provider 链切换 |
| 幂等 | ❌ 无 | 后端全仓 0 命中（仅 liuye 新契约 schema 有字段未实装） |
| 并发 | 半 | sync def 线程池 + SSE 长连占 worker；无队列/限流 |
| 错误处理 | 有 | mount 降级、ledger silent-fail、typed banner |
| 可观测 | 有基础 | /metrics、/health/extended、run_id 三层审计；无 tracing |
| 编排 | ❌ 文档路径 | 闭环 Agent1→6→3→4→5 无代码实现；handoff 曾断 | 
| LLM 层 | 双轨 | llm.py（9 provider）与 shared/llm_caller（4 provider）并存，v16/channel 直用旧层 |

工程卫生：仓库根 100+ 散件；51 分支仅 20 merged（~31 死分支）；state-snapshot 停更 5-11 与 main 漂移；README dev-branch 指向已死分支；W1 untracked 悬置（checkpoint 2/14）；web 双锁文件（package-lock + pnpm-lock）；demo 弱密码明文（users.py，decisions-log 既有 TODO 未做）。

## D. 前端逐页走查台账（生产 liuye.me · 截图在 audit-shots-260720/）

### 横切（多页复现）
| # | 发现 | 截图 |
|---|---|---|
| U1 | 右下角浮动控件（画布/主题/LLM 状态）三 pill 堆叠遮挡内容：报告页盖模板列表、archive 首页盖「打开」按钮 | 01-report-idle、archive-home-1top |
| U2 | 工程话术印满界面：「真 LLM+真 9 维 QC · PM 2026-05-10 真意 reframe」「POST /api/alert/scan → run_scan_and_persist」「持久化 data/alert/sessions/ + ledger 上链」「MAX_ROWS 50000 · numpy backtest · LLM dsl_gen」「mock 只能 mock 输入·不能 mock 结果」「/api/audit 接口（Phase 2 接线）」「0 占位 · 28 残留」 | 01-report-idle、archive-alert-1top、archive-riskctrl-1top、archive-compliance-1top、audit-1top |
| U3 | 模式指示矛盾：报告页同屏「真实数据」pill vs「示例模式」vs「V16 PIPELINE · (MOCK)」 | 01b-after-dp001 |
| U4 | 数字口径互相打架：导航徽章「任务 04·12」vs warroom「0 张可见」；today 消息「15」vs「未读 09」；报告页头统计三空杠 vs 获客页 164/32.8%（疑 mock）vs archive 卡片 27/12/08（疑 mock） | warroom-1top、today-1top、archive-home-1top |
| U5 | 空态无行动引导：dispatch 三栏 90% 空白、warroom 四列全空、customer-list 「暂无客户·切换 [全行客户] 看全部」给提示不给按钮、audit 「暂无事件」 | dispatch-1top、warroom-1top、customer-list-1top、audit-1top |
| U6 | 01./02./03. 装饰脚手架编号；「00户」零填充；中英混排（「正在 flight.」「EVIDENCE-GROUNDED」「板块: corporate」） | today-1top、warroom-1top、08-regen-lower |

### 逐页
| 页 | 发现 | 截图 |
|---|---|---|
| /today | 消息卡第二行被裁切（王审贷·QC 终审 半行）；15 vs 未读 09 口径混乱；01/02 编号；活跃客户 00户 | today-1top |
| /dispatch | 工程测试残留会话「SLO-1 verify · 未分配客户 · 06-15」暴露生产；三栏空 | dispatch-1top |
| /warroom | 四列全空无 CTA；导航 12 vs 页面 0；第四列深色底与整体视觉断裂；HandoffTicket 英文斜体 | warroom-1top |
| /customer/list | 85% 空白；提示文字含工程方括号 [全行客户]；「走访闭环第 1 步」内部黑话 | customer-list-1top |
| /audit | 空事件流；后端审计历史查询未接线（run_id 资产埋没）；界面自述 Phase 2 | audit-1top |
| /archive | ✅ 全站最佳页（六卡+统计即导航+每卡唯一 CTA）；但主题浮球压「打开」按钮、卡片数字疑 mock | archive-home-1top |
| /archive/report | 「开始生成」初始 disabled 无解释；点 DP001 加载出旧会话鼎盛商贸（标签与行为不符）；状态行自相矛盾（QC 阻断 vs 字段覆盖 100%）；头部「请选择客户」与已加载会话不同步；导出 422 契约错误+原始 JSON 糊脸；右侧主区大空白孤挂「重写本章」；时间线章节跳号（§一二三五无四）；重新生成点击后 75 秒页面无任何变化（无反馈） | 01-report-idle、01b-after-dp001、05-after-export-click、08-regen-lower、06-regen-t75s |
| /archive/credit | 标题=机制罗列（4 维评分+红线+案例+决策建议书）；「演示 ≠ 假数据」防御话术；四个虚线空盒占屏；✅ 导出正确禁用（与报告页行为不一致——不一致本身是问题） | archive-credit-1top |
| /archive/riskctrl | 副标题露「KS 0.00 / 通过 0%」零态数字；「不是切假数据」话术；状态栏配置常量 | archive-riskctrl-1top |
| /archive/alert | 整卡 BACKEND 路径/文件目录印在 UI；「真 Tavily + 真 LLM」CTA 文案 | archive-alert-1top |
| /archive/compliance | **页大标题未渲染**（眉标下只有孤立「·」，对比他页大标题）；「监管原文 hash + freshness」字段名上 UI | archive-compliance-1top |
| /archive/channel | 头部统计 164/32.8%/6.2min 来源存疑；channel-kb/ 文件路径上 UI；KB 三卡上传+查询框本身 ✅ 不错 | archive-channel-1top |

### 报告时间线（正面资产 + 问题并存）
左栏时间线（上传→解析→AI 提问→客户经理回复→分段生成带证据率→QC 终审→HITL 确认请求）是**全产品最好的产品叙事**（shots2/08-regen-lower）；问题：右侧主区空白失衡、章节跳号、「分析结论 · EVIDENCE-GROUNDED」英文标签。

## E. 生产探针记录（260720 04:00）
- `GET /api/credit/presets/corporate` → 200（生产 nginx 全代理，dev rewrite 缺口是 dev-only 债：next.config.ts:29-45 只代理 report/auth/credit，channel/alert/compliance/riskctrl 本地 404 被 canned 回复掩盖 = dev 假成功，ChannelWorkspace.tsx:350）
- `GET /api/channel/run` → 405（路由存在）
- `GET /api/report/health` → 200 `llm_connected:true`（8002 报告后端活）
- 导出实测 → 422 `pending_questions` dict_type 校验失败（契约漂移）
- 浏览器 console：1 错误（422 同源）

## F. 已知问题台账要点（docs/ 既有记录，未修）
- F-005 Scout 自由搜索「NEVER CORRECTLY DELIVERED·待重做」；F-001~008 Phase C 恢复批 pending
- Prompt SSOT 0 注入（8 段 SKELETON 全 _PENDING，自评 D 级）
- DoD 自评 L0 65% / L1 55% / L2 60% / L3 30%，总评 C+
- 5 个 pre-existing test failures（alert/riskctrl）
- 时间线断点：文档全部止于 2026-05-12，其后 v16-rewrite/report-api 修复未入 snapshot

## G. 保留资产清单（升级不许打碎）
确定性判断层、规则外置 JSON、SSE 证据事件协议（sse_envelope 共形）、run_id 三层审计、RBAC+JWT+bcrypt、empty-state protocol + IdleScaffold、LiveFailError 禁静默降级、data_source 5-enum SSOT、ModePill、evaluation YAML、v16 模板分解（TemplateRole/防泄漏指纹）、报告时间线叙事、archive 首页设计、乾策 Studio 视觉语言与 4 主题。
