# 演示冲刺 · 全方位优化执行方案（Codex 执行版）

> 260721 · 明天演示。执行者：Codex CLI（workspace-write）。方案作者：Claude（基于四轮对抗审 + 260720-21 全仓审计与生产实测，证据见同目录《审计报告-260720.md》《升级方案-融合有数知识体系-260720.md》）。
> **两条主线**：A 线 = 用有数已跑通的经验做代码层补强；B 线（更重要）= UI bug 修复与演示效果。
> **总原则：明天演示成功是第一性。所有改动按"是否让演示更可信、更顺滑"排序；改变行为的必带负例测试；每 Wave 结束部署验证，坏了立刻回滚。**

---

## §0 硬约束与成功定义

- **演示环境 = 生产 liuye.me**（nginx 全代理，全 Agent 路由通）。**绝不用本地 dev 演示**（dev 有 rewrite 缺口：channel/alert/compliance/riskctrl 本地 404 被假成功掩盖）
- 登录：演示账号 u_liuye（admin，密码见 auth_service/users.py 注释规则）。**本轮不改密码**（演示前夜不动登录链路）
- 部署：`bash scripts/deploy_to_ecs.sh`（含 npm build 5-10 分钟；build 失败服务不重启、线上不掉——安全）。**Codex 沙箱跑不了 SSH 时，deploy 由刘野手跑或单独用 danger-full-access 执行这一条**
- 成功定义（三条全满足）：①演示动线（§2 七幕）在生产站连跑两遍零穿帮 ②A 线负例测试全绿进回归 ③生产 8 页工程话术 grep 保持零（260721 已达成，不许回退）

## §1 必读资产与移植哲学

开工先读（按序）：
1. 本文件全文
2. `docs/upgrade/审计报告-260720.md`——所有 bug 的行号级证据
3. `docs/upgrade/升级方案-融合有数知识体系-260720.md` §2-§4——修法设计与验收契约
4. 仓库 `CLAUDE.md` §3（工程规范）——遵守既有纪律

**有数经验四原则（A 线的移植内核，改动时对照）**：
1. **缺数不装懂**：缺失字段显式标「未能自动填写」+ 进待办，绝不生成事实型正文（缺失四态思想）
2. **验证不过不产出**：质检失败的产物出门必须带阻断标识（发布门思想，今晚落最小版=水印草稿）
3. **演示与判断层物理隔离**：演示数据只能做输入，永不覆盖判断输出
4. **状态单一真源**：同一事实（QC 结论/任务数/模式）全站只有一个推导源

## §2 演示动线（七幕故事 · 给刘野的叙事骨架，也是 Codex 的验收剧本）

> 主线叙事：「一个客户经理的一天」。三个**卖点时刻**（由今晚的修复直接创造）用 ★ 标出。

| 幕 | 页面 | 动作 | 讲什么 |
|---|---|---|---|
| 1 开场 | /today | 看今日看板 | 待办/预警/客户一屏；数字全真不打架（B4 修完后） |
| 2 生成 | /archive/report | 点 DP001 龙峰精工 → 开始生成 → 看五段流水 | 材料解析→字段抽取→段落生成→QC 终审全程可见 |
| 3 ★缺数不装懂 | 同页 | 指着「未能自动填写」灰条+待办清单 | 「查不到的字段系统不编——这是银行敢用的前提」（A1 创造）|
| 4 ★质检拦截 | 同页 | 演示 QC 阻断态点导出 → 得到水印草稿+阻断原因页 | 「质检不过，正式报告出不了门」（A2 创造）|
| 5 决策 | /archive/credit | 示例数据·鼎盛商贸 一键决策 | 评分/红线/案例/建议书全链，红线命中展开讲 |
| 6 风控面 | /archive/alert → /archive/compliance | 各跑一次示例扫描 | 批量扫描红黄绿分级；政策逐条比对 |
| 7 ★全程留痕 | /audit | 看当日操作事件流 | 「刚才每一步都在这里，可追溯」（彩排时先跑操作攒事件，见 §7）|

## §3 A 线任务卡 · 代码补强（有数经验移植 · 全部带负例测试）

> 通用纪律：每卡 = 先写失败测试（`tests/upgrade/`，`py -3 -m pytest` 跑红）→ 修 → 跑绿 → 单独 commit。测试断言写死在卡里，不许放宽。

### A1 · 拆除缺数造词（对应原则 1）｜预计 60-90min
- **锚点**：`v16_op_handlers.py:946-999` `_DEMO_SAFE_FALLBACK` 42 键字典（:948 注入点）；`:209-214` kb_lookup_fill「（详见补充材料）」回退
- **修法**：删字典及消费逻辑；缺值统一 `UNFILLED_MARKER`（`shared/evidence/protocol.py:21` 已有常量，import 复用）；确认缺值字段进 pending_tags 清单（查 `v16_generator.py:833-872` 生成处，「未能自动填写」须被计入）
- **⚠️ 前置检查（拆之前必做）**：跑 DP001-005 五家示例各一遍生成（或查 outputs/ 最近产物），统计每家「未能自动填写」数量。**选缺值最少的一家定为演示主角**（默认 DP001 龙峰精工，若它缺值多则换）并把结论写进 §7 彩排清单。缺值不是丢人的事（第 3 幕卖点），但主角页面不能刷屏
- **测试** `tests/upgrade/test_no_fabrication.py`：①构造缺值 case 调 handler，断言输出零命中「本企业|成立多年|详见证照|符合本行准入|详见补充材料」且含 UNFILLED_MARKER ②模块级断言 `_DEMO_SAFE_FALLBACK` 不存在
- **回归**：`py -3 -m pytest tests/integration/test_render_smoke.py`（9 例必须保持全过——注意其名含 no_naked_placeholder，若它把 UNFILLED_MARKER 当裸占位符拦，改测试的判定注释说明语义：显式标记≠裸占位符）

### A2 · 导出装闸 = 发布门最小版（对应原则 2）｜预计 90-120min
- **锚点**：`agent_report/api.py:1356-1415` export_docx；`:1393-1394` 与 `:1809-1810` 两处 `if req.qc is not None: payload["qc"] = req.qc`（客户端可伪造 QC）；`ExportDocxRequest :1243-1249`（pending_questions 类型窄导致生产 422）
- **修法**：
  1. 服务端**忽略**客户端传入 qc/sections/profile（接受不采信——字段保留防炸旧前端）；QC 判定一律取 SessionStore 会话内 qc_payload；无记录=未质检
  2. 失败策略确定化：qc.passed==False 或未质检 → docx 首页页眉水印「质量闸未过 · 内部草稿 · 不得作为审批依据」+ 文首阻断原因段（block/warn 明细，python-docx 后处理）；通过 → 正常导出。export_pdf（:1798 起）同型同修
  3. 422 修复：pending_questions 宽容化（list|dict|None 归一）；错误响应人话 message（前端横幅直显，不能糊 FastAPI 原始 JSON）
- **测试** `tests/upgrade/test_export_gate.py`（FastAPI TestClient + mock SessionStore）：①伪造 `qc={"passed":true}` + 会话实际 blocked → 产物解包验页眉含水印文字 ②会话通过 → 无水印 ③pending_questions 传 dict → 非 422
- **演示衔接**：第 4 幕靠此卡。彩排时确认演示会话确实处于阻断态可展示水印稿（若五家示例全通过，用「福建普惠授信申报及审查审批意见表」那个 WARN 模板会话制造阻断态）

### A3 · 合规判断层去预埋（对应原则 3）｜预计 45-60min
- **锚点**：`agent_compliance/agent.py:314-325`——场景模式 `ledger.normal.clear()/observation.clear()` 后重放 scenario.json planted 清单（真实检出被丢弃）
- **修法**：删 clear+重放；planted 若为事件形态则作为输入走真规则引擎，若为结论形态整段删除；demo 与 live 同输入必须同违规集（仅展示标签不同）
- **测试** `tests/upgrade/test_compliance_no_planted.py`：同输入双模式跑，断言违规 rule id 集合一致；断言源码无 `ledger.normal.clear()`
- **⚠️ 演示回归**：改完在生产跑一次「示例扫描」，确认违规明细仍有内容且合理（预埋拆除后如果真实规则检出为空，演示第 6 幕会空屏——若发生，加真实可触发的示例输入事件，不是加回预埋结论）

### A4 · 授信输入去魔数（对应原则 1）｜预计 30-45min
- **锚点**：`agent_credit/agent.py:114` `or 300`；`:108` 员工数抽取失败落 0
- **修法**：额度缺失 → None + 决策建议明示「申请额度未提供，以下仅为风险评估」且不出额度测算数字；员工数 None 时评分跳过该特征不按 0
- **测试** `tests/upgrade/test_credit_no_magic.py`：无额度 case 断言输出无「300」测算且含声明；员工数缺失断言不按 0 计分
- **演示回归**：鼎盛商贸 demo case（`demo_data/agent_credit/corp_dingsheng_trade.json`）字段齐全不受影响——跑一遍确认第 5 幕正常

### A5 · gates.md 落笔｜5min（纯文档卡 · TDD 例外，验收=红绿 grep 断言）
- `docs/upgrade/gates.md`：GATE-0A-1/0A-2 状态改 PASS，批准人刘野，批准日 2026-07-21，备注「口头授权演示冲刺执行 P0（A1-A4 即 P0a+P0b 核心）」
- **验收形态（CP1-BLOCKED 修正 · 260721）**：本卡不适用"失败测试"纪律。红态 = 改前 `grep -E "GATE-0A-[12].*OPEN" docs/upgrade/gates.md` 命中 2 行（当前即红）；绿态 = 改后同命令命中 0 行 **且** `grep -cE "GATE-0A-[12].*PASS.*2026-07-21"` = 2。两条 grep 结果贴进 commit message
- **工具挂起替代姿势（全 Stage 通用）**：apply_patch/编辑工具挂起时，改用 `py -3` 内联脚本读改写文件（io.open utf-8, newline='\n'），不要重试挂起的工具超过 1 次

## §4 B 线任务卡 · UI 修复与演示效果（更重要）

### B1 · 报告页示例加载语义修复（生产实锤 U8）｜预计 45-60min
- **现象**：点 DP001 卡加载出**旧会话**（鼎盛商贸），标签与行为不符；随后「重新生成」把会话整个换成「测试样本有限公司 · DEMO 模式」——公司、模式、QC 结论全变
- **锚点**：`ReportWorkspace.tsx` handleDemoRun(:527 起)/triggerV16Fill(:228 起)；后端 `/api/report/demo/run` sample_id 消费与 SessionStore 复用逻辑（agent_report/api.py 内搜 demo/run）
- **修法**：①示例卡点击 = 明确为该企业新起会话（不复用上一会话）②「重新生成」必须重跑**当前会话同一企业**，无材料时不许静默 fallback 到默认 docx/mock（宁可报「请先选择示例或上传材料」）③顶部「真实数据/DEMO 模式」pill 与会话来源严格同步（数据源单一：会话对象上的 mode 字段）
- **验收**：生产实测——点 DP001→页头企业=龙峰精工；重新生成→仍是龙峰精工；模式标全程一致

### B2 · 生成过程即时反馈（生产实锤：点击后 75s 界面零变化）｜预计 30-45min
- **锚点**：ReportWorkspace 生成触发后 liveStages 渲染链
- **修法**：点击后立即进入「生成中」视觉态（按钮转菊花+五段流水条第一段点亮+右栏骨架屏）；SSE 事件到达前显示「正在连接生成服务…」；失败显式横幅（已有 LiveFailError 体系，接上）
- **验收**：生产点「开始生成」，1 秒内界面有明确响应

### B3 · 导航假数字全清（重大穿帮：Masthead 硬编码）｜预计 30min
- **锚点**：`web/src/components/shell/Masthead.tsx:22`——「今日 01 / 对话 02·3 / AI 助手 03·6 / 任务 04·12」全是**硬编码装饰字符串**，与页面真实数据冲突（warroom 实际 0 张）
- **修法**：数字徽章要么接真数据源（ticket store/dispatch 会话数——如果 30 分钟内接不通），要么**全部去掉数字只留文字标签**（推荐，演示安全第一）。保留编号风格可用「01/02/03/04」纯序号（不含数据语义）
- **验收**：导航任何数字与对应页面实数一致，或无数字

### B4 · today 口径统一 + 演示数据剧本化｜预计 45-60min
- **锚点**：`MorningBrief.tsx` stats（tickets 来源 mock fallback）；消息卡「15」vs「未读 09」
- **修法**：①大数字与徽章同源（都用未读数或都用总数，标签写清）②演示种子数据剧本化：消息/进行中任务/最近事项围绕演示主角企业改写（龙峰精工生成中、鼎盛商贸待决策、示例预警户），让第 1 幕与后续幕互相呼应③「进行中 IN FLIGHT」卡的企业名与百分比在演示动线里真实存在
- **验收**：today 页所有数字可解释、企业名与后续演示页一致

### B5 · dispatch 生产脏数据清理 + 种子会话｜预计 30min
- **现象**：生产对话页唯一会话=「SLO-1 verify · 未分配客户 · 06-15」工程残留
- **修法**：ECS 上清除该会话数据；种子 1-2 个演示会话（如「龙峰精工 · 授信材料补充」含 2-3 条像样对话）。数据在服务端存储（查 im_service 持久化位置）
- **验收**：生产 dispatch 列表无工程会话、有演示会话

### B6 · warroom 空态与种子 ticket｜预计 30-45min
- **修法**：①空态加引导文案（各助手产生的交接任务汇入此处）②种子 2-3 张演示 ticket（龙峰精工报告→审贷交接等，与动线呼应）；ticket 存储在 mock store 则改 mock 数据文件
- **验收**：warroom 四列有演示内容可拖拽；与导航徽章口径一致（B3）

### B7 · 浮动控件堆叠修复（画布/主题/LLM pill 右下角互挡）｜预计 30-45min
- **锚点**：画布 dock、主题按钮、report 状态 pill 的定位样式（三者分属不同组件，全钉右下）
- **修法**：统一右下角安全区栈（垂直排列固定间距，或主题/画布收进一个折叠按钮组）；**不改视觉风格只改布局定位**（Q-047 逻辑层）
- **验收**：报告页/archive 首页右下角三控件互不遮挡、不盖内容列表

### B8 · channel 统计假数零态化｜预计 20min
- **锚点**：ChannelWorkspace 头部 164 / 32.8% / 6.2min（来源存疑，审计判 mock 常量）
- **修法**：确认数据源；mock 常量则零态显示「—」（与报告页统计位一致）
- **验收**：未运行过的统计位不显示具体假数

### B9 · 报告预览正文 (mock) 前缀清理｜预计 20min
- **锚点**：`web/src/lib/mock/agent-report-session.ts` 预置会话文案每句带「(mock)」前缀（生产截图实锤）
- **修法**：mock 会话文案去 (mock) 字面（模式标识由 ModePill/水印承担，不由正文承担）；顺带把该文件里演示文案里的占位符残留（如 {T2}类）清一遍
- **验收**：预览正文无 (mock) 字面

### B10 · audit 页演示可用性｜预计 15min（+彩排配合）
- **修法**：本卡不做后端接线（时间风险），只确保：①空态文案已人话（已完成）②事件确实随操作写入当前会话流（验证既有 event-bus 在生产可用）。**第 7 幕的内容靠彩排时先跑操作攒事件（§7）**
- **验收**：生产跑一次报告生成后 audit 页出现对应事件

## §5 分段执行回路（指挥链：Claude 决策官 → Codex 执行器 → Claude 验收 → 放行/返工）

### 5.0 角色与权限分离（harness 根保证）

| 角色 | 权限 | 禁止 |
|---|---|---|
| **Claude（决策官）** | 审 diff / 复跑测试 / **按卡分块 commit（A 线五卡文件域零重叠，分块无歧义）** / push / deploy / 生产实测 / 判定 PASS·FIX·REPLAN / 改方案 | 不代写 Codex 的实现（保持独立验收视角） |
| **Codex（执行器）** | 按当前 Stage 的卡写代码+测试+跑验收；**每卡完成后在 execution-log「卡报告」节追加一行**：`卡ID | 改动文件清单 | 验收命令与结果末行` | ❌ **git 一切写操作（add/commit/push——沙箱禁写 .git，260721 实证）** ❌ deploy ❌ 跨 Stage 抢跑 ❌ 放宽卡内写死断言 ❌ 触碰 §6 清单 |
| **刘野（信使+终裁）** | 每段两个动作：①粘 Stage 指令给 Codex ②Codex 停下后回到 Claude 会话说「CP-N」 | — |

### 5.1 段位与检查点（CP = Claude 强制验收闸，不过不进下一段）

```
Stage 1 · 后端诚信线（A5→A1→A2→A3→A4）          ≈4h
  └─ CP1（Claude）：亲读全部 diff · 复跑 4 个负例测试文件+render-smoke ·
     核对断言与卡内写死一致（防被放宽）· 定演示主角企业（A1 覆盖率结论）
     → PASS 才允许进前端 ｜ FIX 出修正卡返工 ｜ REPLAN 改方案
Stage 2 · 前端核心（B1→B2→B3→B9）               ≈2.5h
  └─ CP2（Claude）：tsc 干净 · 亲读 diff · 我 push + deploy Wave-1 ·
     生产实测：动线前四幕跑通 + 工程话术 grep 保持零 + 截图目检
     → 前四幕有任一幕断 = 先修再进 Stage 3
Stage 3 · 演示数据与周边（B4→B5→B6→B7→B8→B10）  ≈2.5h
  └─ CP3（Claude）：我 push + deploy Wave-2 · 生产跑全动线七幕（Playwright
     截图存档）· 数字口径全站核对（导航=页面=数据）
Stage 4 · 彩排与冻结                              ≈1h
  └─ CP4（Claude）：出《彩排报告》给刘野（每幕状态/风险/备用路径）·
     演示日早 8 点冻结——之后只许 git revert 不许新增
```

### 5.2 每 CP 的固定验收动作（Claude 逐条执行，不听转述）

1. `git log --oneline` 对照卡清单——每卡一 commit、无多余改动面
2. `git diff <stage 起点>..HEAD --stat` + 逐文件亲读关键 diff
3. A 线：`py -3 -m pytest tests/upgrade/ tests/integration/test_render_smoke.py -q` 亲跑；断言逐条对照 §3 卡内写死内容
4. B 线：`pnpm exec tsc --noEmit`；部署后用生产站真机截图目检（不接受 Codex 自述截图）
5. 判定写入 `docs/upgrade/execution-log-260721.md`（唯一进度真源）：`CP-N | 判定 | 证据 | 修正卡（若有）| 放行时间`
6. **返工回路**：FIX 判定 → Claude 出修正卡（精确到文件行号与期望行为）→ 刘野粘给 Codex（resume 同会话）→ 复验。**同一卡返工 ≥2 次 → 触发降级方案或砍卡（§5.4 砍卡序）**

### 5.3 部署与回滚（Claude 专属动作）

- push：`git push origin main`（网络抖动重试；>2 分钟挂起属正常，后台等）
- deploy：`bash scripts/deploy_to_ecs.sh`（render-smoke 9/9 必须过；build 失败线上不掉）
- 生产验证：8 页工程话术 grep 清零保持 + 动线实测截图
- 回滚：`git revert <卡 commit> && push && deploy`；演示日早 8 点后冻结

### 5.4 时间不够的砍卡序（保命优先级，从后往前砍）

B8（channel 假统计）→ B6（warroom 种子）→ B7（浮动控件）→ B5（dispatch 种子）→ A4（授信魔数）——**A1/A2/B1/B2/B3 五卡不可砍**（三个卖点时刻 + 两个必穿帮点全系于此）。砍卡即时记入 execution-log 并同步刘野。

### 5.5 通用禁区（原§5 第 6 条保留）

不动 auth/密码链路；不动视觉 token 与主题；不删 data-testid；不动 §6 清单；ECS 上不改 .env；Codex 不 push 不 deploy。

## §6 明确不做（防跑偏——这些有价值但演示前动=风险大于收益）

- 平账闸 47 勾稽移植 / 字段级证据台账持久化 / claim verifier 代码移植（IP gate 未过 + P1 大件）
- MCP 数据接入（离线演示不需要 + IP gate）
- LLM 层唯一化 / job runtime / api_server 拆分（harness 大件）
- 密码轮换（演示后做）
- dev rewrite 缺口修复（演示用生产，不急）
- 三段式证据阅读室 / 发布门完整四态状态机（演示后 P1）

## §7 彩排 checklist（演示前 2 小时 · 刘野亲自过）

1. liuye.me 登录 u_liuye → today 开场页目检（数字口径、企业名与动线一致）
2. report：点【A1 选定的主角示例】→ 生成全程 → 确认「未能自动填写」呈现得体（第 3 幕）→ QC 结果
3. 制造/确认一个 QC 阻断会话 → 点导出 → 拿到水印草稿（第 4 幕）→ 再演示一个通过会话正常导出
4. credit：鼎盛商贸一键决策 → 红线明细展开
5. alert 示例扫描 → 红黄绿榜单 → 点开一户看证据链
6. compliance 示例扫描 → 违规明细（A3 后必须非空）
7. audit 页确认以上操作已留痕（第 7 幕素材就位）
8. 全程 F12 console 零红错；浮动控件不挡内容
9. 备份路径：演示机提前登录留 session；备一台热备浏览器窗口；网络断则截图兜底（彩排时全程截图存档）

## §8 分段启动指令（刘野按 CP 节奏逐段粘贴 · 一次只发一段）

**Stage 1（现在发）**：
```
codex exec --sandbox workspace-write -C "D:\claude code\credit_report_agent_work" "读 docs/upgrade/演示冲刺-Codex执行方案-260721.md 全文（重点 §1 必读资产、§3 任务卡、§5 分段回路）。你是执行器，本段只执行 Stage 1 = A5→A1→A2→A3→A4 五卡：每卡先写卡内写死的失败测试→跑红→修→跑绿→单独 commit（本地 commit，禁止 git push、禁止 deploy、禁止碰任何 B 卡与前端文件）。A1 的前置覆盖率检查结论（演示主角企业选谁）写入 docs/upgrade/execution-log-260721.md。全部完成或遇到无降级方案的阻塞时停下，输出：每卡一行结果（commit hash + 测试结果摘要）+ 阻塞清单。不要自由发挥扩大改动面。"
```

**Stage 2 / Stage 3 指令**：由 Claude 在 CP1/CP2 判定 PASS 后按当时实况生成（含上一段验收发现的修正项），刘野照粘即可——不预写，防止方向已偏还按旧指令跑。

**CP 交接动作（刘野）**：Codex 停下后，回 Claude 会话说「CP1」（或粘 Codex 尾部输出），Claude 开始验收。

---
*方案依据：docs/upgrade/ 四轮对抗审文档链 + 260720-21 生产实测（25 截图）+ 260721 已上线第一波修复（7 commit，工程话术 grep 已归零——本方案是第二波，不许回退第一波成果）。*
