# 施工卡 B1/B2 · 形态模式封口（上线前阻断项，2026-09-08 上海时间凌晨）

来源：Astra 上线前审查（`D:\工作交付\面试演示-数字人岗-260907\说辞\astra-预部署审查-260908.log`）B1、B2。分支 `demo-form-260907`，工作树已有 44 个文件处于暂存态（上一卡产物），**不要 unstage、不要 commit**；本卡改动完成后由 CC 重新暂存。

## B1 形态模式必须在后端统一封口
现状：`DEMO_FORM_MODE=1` 只拦 `POST /api/report/demo/run`（`agent_report/api.py:1641`）。章节重写（`agent_report/api.py:1160–1257`）、聊天（`api_server.py:720`，且不强制登录）、材料/模板上传删除（`agent_report/api.py:448–479、698–735`）、反馈写 JSONL（`api_server.py:460–493`）、各 Agent 的示例扫描/一键决策等仍可触发模型调用或服务器写入。

要求：
1. 在 `api_server.py` 增加一个 ASGI/FastAPI 中间件（或统一依赖）：当 `DEMO_FORM_MODE=1` 时，**所有非 GET/HEAD/OPTIONS 请求默认 403**，响应体 `{"detail":{"error":{"code":"DEMO_FORM_READONLY","message":"演示环境为只读形态 · 已停用生成、上传与写入"}}}`；**白名单**只放行：`/api/auth/*`（登录登出）、`POST /api/report/export_docx`、`POST /api/report/export_pdf`。其他 POST/PUT/PATCH/DELETE 一律 403（含所有 agent 的 demo/run、scan、decide、rewrite、upload、template、feedback、IM 发送）。
2. 前端：形态模式下 `ComposerBar`（对话页发送）与任何仍可见的「运行示例扫描 / 一键运行 / 启动风险扫描 / 重写本章」按钮改为禁用态并显示同一句人话说明（不改布局），避免访客点了看到裸 403。
3. 负例测试 `tests/upgrade/test_demo_form_readonly.py`：开关开时 ①`POST /api/report/demo/run` 403 ②任一 rewrite/上传接口 403 ③聊天接口 403 ④`export_docx` 仍 200 ⑤登录仍 200；开关关时以上行为与现状一致（回归）。

## B2 既有认证旁路
现状：`im_service/auth.py:76` 真实认证失败后接受旧演示 token；`api_server.py:795` 用其解析 IM 身份；`agent_report/api.py:387`、`:1515` 的下载接口无登录依赖。

要求：
1. 旧演示 token 回退：默认关闭（改为需显式 `ALLOW_LEGACY_DEMO_TOKEN=1` 才启用），形态模式下强制关闭。
2. 两个下载接口加登录依赖（复用现有的用户依赖），并校验下载对象属于当前会话/用户能访问的范围；无会话则 404 而非裸文件。
3. 负例测试：旧 token 在默认配置下 401；未登录下载 401；登录后下载 200。

## 硬规则
- 不改 QC / 判断逻辑，不动 seed 脚本与已暂存的 44 文件之外的无关文件；每改一个既有文件先 `git diff --stat -- <file>`，若该文件在暂存集内则直接在工作树上改（CC 会重新 add）。
- 全量：`py -3 -m pytest tests/upgrade -q` 与 `tests/integration/test_render_smoke.py` 不得变红；`pnpm exec tsc --noEmit` 0 错。
- 回执写 `docs/demo-form/施工回执-B1B2-260908.md`：改动文件清单、每条验收实取输出、未做到项。不 commit。
