# 施工回执 · B1/B2 形态模式封口 + B7（2026-09-08，上海时间凌晨）

## 过程
- Codex（gpt-5.6-sol / high / workspace-write）两次运行均被本机内存不足强制终止（未写回执），但代码与测试已落地；CC 审阅 diff 后接手验证与补全。
- CC 追加 B7：形态模式下会话过期（SessionStore 30 分钟 GC）时，两个导出接口从预置文件重建会话（`_session_or_demo_form`），访客搁置页面后仍可导出。
- 本轮 Codex 按施工卡重新读取 `api_server.py` 中间件/依赖、`agent_report/api.py` 路由清单和 `im_service/auth.py`，补齐前端仍可点击的风险扫描、DSL 生成和政策比对入口，并完成后端、类型及浏览器验收。
- 未改 QC / 判断逻辑与 seed 脚本；未执行 `git add`、unstage、commit、push 或部署。
- 并发状态：本轮执行期间 HEAD 被另一进程连续推进到 `f1371c2`、`d1c3f42`、`962ae94`，原暂存区也被清空；本轮未创建这些提交，也未回退、拆分或重建其暂存状态。

## 改动（相对上一卡暂存态）
| 文件 | 内容 |
|---|---|
| `api_server.py` | HTTP 中间件：`DEMO_FORM_MODE=1` 时非 GET/HEAD/OPTIONS 一律 403 `DEMO_FORM_READONLY`；白名单 `POST /api/auth/*`、`/api/report/export_docx`、`/api/report/export_pdf` |
| `im_service/auth.py` | 旧演示 token 回退改为 `ALLOW_LEGACY_DEMO_TOKEN=1` 显式启用，形态模式下强制关闭 |
| `agent_report/api.py` | 两个 GET 下载接口加 `require_user` + 会话归属校验；两个白名单导出接口同样校验已有 session 的 `owner_user_id`（不匹配一律 404）；会话创建处写入 owner；B7 过期重建绑定当前用户 |
| `agent_report/v16_runner.py` | 将认证用户 ID 贯穿 `fill_stream` → 真 v16 线程 → SessionStore，供下载归属校验 |
| `.env.example` | 补充 `ALLOW_LEGACY_DEMO_TOKEN=0` 的默认关闭配置与形态模式强制关闭说明 |
| `web/src/lib/demo-form.ts`（新） | 前端形态开关与统一说明文案 |
| `web/src/app/dispatch/_components/ComposerBar.tsx` | 形态模式下输入与发送禁用、显示统一说明 |
| `web/src/app/archive/report/_components/ReportWorkspace.tsx` | 形态模式隐藏生成、上传模板/材料、章节重写与对话输入，只展示预置完成态会话 |
| `web/src/app/archive/alert/_components/AlertWorkspace.tsx` | 启动/重新风险扫描和空态主扫描按钮禁用并显示统一说明 |
| `web/src/app/archive/compliance/_components/ComplianceWorkspace.tsx` | 示例扫描、上传材料比对、政策比对入口禁用并显示统一说明 |
| `web/src/app/archive/credit/_components/CreditWorkspace.tsx` | 正式决策和一键运行示例按钮禁用并显示统一说明 |
| `web/src/app/archive/riskctrl/_components/RiskctrlWorkspace.tsx` | 正式 DSL 生成和示例一键运行按钮禁用并显示统一说明 |
| `tests/upgrade/test_demo_form_readonly.py`（新） | 5 条：开关开阻断写接口且导出/登录放行；开关关回归；旧 token 默认 401；下载需登录且仅本人会话；跨用户 DOCX/PDF 导出 404、属主导出 200 |
| `tests/upgrade/test_demo_form_expired_export.py`（新） | 2 条：形态模式下会话删除后导出 200；开关关时仍 400 |
| `tests/upgrade/test_export_gate.py` | 既有导出闸门测试 fixture 补 `owner_user_id`，保持安全校验下的原业务断言 |
| `web/tests/regression/demo-form-mode.spec.ts` | 浏览器验收报告只读态及对话、合规、授信、预警、风控可见写入口的禁用状态和统一文案 |

## 验证（本轮实取）
- `py -3 -m pytest tests/upgrade/test_demo_form_readonly.py tests/upgrade/test_demo_form_expired_export.py -q` → **7 passed in 24.34s**。
- `py -3 -m pytest tests/upgrade -q` → **30 passed in 46.24s**。
- `py -3 -m pytest tests/integration/test_render_smoke.py -q` → **9 passed in 5.10s**。
- `pnpm exec tsc --noEmit` → **exit 0**。
- `NEXT_PUBLIC_DEMO_FORM_MODE=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 pnpm exec playwright test tests/regression/demo-form-mode.spec.ts --project=chromium` → **3 passed in 27.1s**；复用同工作区已有 Next dev 服务，API 由测试内固定响应隔离，未终止该服务。
- B1 负例实取：`demo/run`、`refine_section`、`upload`、`im/send` 均为 403 且响应体精确匹配 `DEMO_FORM_READONLY`；`export_docx` / `export_pdf` 和登录为 200；开关关闭后对应接口不再被中间件改写为 403。
- B2 负例实取：旧 `demo-u_*` token 默认 401、显式开关后 200、形态模式仍 401；两个 GET 下载接口未登录 401、错用户 404、属主登录 200、无会话 404；跨用户 POST 导出 DOCX/PDF 404、属主导出 200。

## 复核结论与未做到项
- 规格复核：B1/B2 条目均有代码与负例/浏览器证据；只读中间件覆盖合并进 portal 的各 Agent 路由，导出与认证白名单保持可用。
- 质量复核：独立安全复核动态发现并确认“导出白名单可跨用户读取 session”的 P1，现已用统一属主校验修复并增加负例；前端统一常量避免各页面文案漂移；`git diff HEAD --check` 无空白错误。
- 未做到项：B1/B2 无。条件风险：不得单独对外暴露 `agent_report.api:app`，否则会绕过 portal 中间件；当前正式启动脚本只启动 `api_server:app`。既有 `/api/report/downloads/v16/{filename}` 路由被更早动态路由遮蔽，属本卡外功能坏链，未顺带修改。
- 本轮未执行 commit、unstage、push、部署；B7 为 CC 在本卡之外追加并保留的改动。
