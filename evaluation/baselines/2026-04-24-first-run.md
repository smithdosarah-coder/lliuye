# 6 Agent 首轮评估基线报告 · 2026-04-24

**Commit**: `0b47270` (feat/evaluation)
**Schema**: A-024（runner 路径 `evaluation/runner/`）· A-025（YAML 双字段）
**运行方式**: `py -m evaluation.runner --all` + agent6 带 `samples/普惠申报书_骨架型.docx` artifact
**JSON**: [`2026-04-24-first-run.json`](2026-04-24-first-run.json)

---

## ⚠️ 警示（读报告前必读）

本轮基线**偏乐观**——

- **mock 数据简单**：agent3 用 `agent_credit/mock_data/corporate_cases.json` 64 静态 case，字段已预填，不反映真实审贷信息不全场景
- **pending 指标占比高**：agent1 / agent5 全量 pending（无 runtime dump）；agent2 / agent4 半量 pending（真值库未到位）
- **agent6 artifact 退化**：真 v16 管线产出 docx 本地未留存，本轮用 `samples/` 骨架自对比 → `template_leakage_rate=1.0`（非真实 gap）

**结论**：本轮数字作"起点参照"，**非客户证据**。Batch 2 待 data-foundation Batch 2 真脏数据落地后重跑对比 gap。

---

## 一览表

| Agent | verdict | 红线闸门 | 实算条数 | pending 条数 | 主要 gap |
|---|---|---|---|---|---|
| alert | 🟢 PASS | ✅ 全绿 | 6/10 | 4/10 | 待真值库 |
| channel | 🟡 PARTIAL | 🟡 全 N/A | 0/10 | 10/10 | 待 `agent_channel.lead_finder` runtime dump |
| compliance | 🟡 PARTIAL | 🟡 全 N/A | 0/10 | 10/10 | 待 `agent_compliance.policy_scanner` runtime dump + gold 条款集 |
| credit | 🟡 PARTIAL | ✅ 全绿 | 6/10 | 4/10 | tool 埋点 + 术语表 + 人工复核真值 |
| report | 🔴 FAIL | 🟡 部分 N/A | 3/10 | 7/10 | artifact 退化（骨架自比）· 真 v16 产出未入库 |
| riskctrl | 🟡 PARTIAL | ✅ 全绿 | 5/10 | 5/10 | B1 新加 2 条待 adapter 埋点 + Phase 2 LLM-judge |

**红线闸门** = `hallucination_rate` / `evidence_rate` / `task_completion_rate` 三闸是否全绿。

---

## 1 · Agent1 获客 (channel) · PARTIAL

- **红线闸门**：🟡 全 N/A（无 runtime dump）
- **实算条数**：0 / 10
- **Gap top 3**：无实算数字可比，全量缺口——
  1. 缺 `agent_channel.lead_finder` runtime dump（候选 + 信号 + 证据）
  2. 缺业务方提供的 look-alike 种子真值集（`gold_lookalike`）
  3. 缺人工 gold 排序（NDCG@10）

**改进建议**：
- **Phase 2 先做 runtime dump** — 在 `agent_channel.api.py` 搜索 endpoint 里埋点把 candidates 写 `evaluation/manual/1_latest.json`，先解 field_completeness / evidence_rate / signal_diversity / candidate_dedup_rate 4 条
- **portrait_match_precision** 走 LLM-judge（复用 Agent6 v16 pipeline 的自审阶段经验）或人工抽检
- **ndcg/retrieval_recall** 业务方提供 Top20 候选真值

---

## 2 · Agent2 风控 (riskctrl) · PARTIAL

- **红线闸门**：✅ 全绿（halluc=0 / evidence=1.0 / task_completion=1.0）
- **实算条数**：5 / 10
- **Gap top 3**：
  1. `per_rule_fpr_spread` = N/A（fixture 中 reject 规则 < 2 条，spread 不适用）
  2. `ks_improvement` / `rule_interpretability` = pending（Phase 2 LLM-judge + baseline_ruleset 对照组未到位）
  3. `field_completeness` / `dsl_syntax_correctness` = pending（B1 新加，adapter 未埋点）

**改进建议**：
- `field_completeness` + `dsl_syntax_correctness` 两条是 adapter 侧工作量——在 `evaluation/runner/adapters/agent2_riskctrl.py` 加：规则 4 键完整率 + parser round-trip 校验
- `ks_improvement` 需 data-foundation Batch 2 真 baseline_ruleset
- `rule_interpretability` 走 LLM-judge 低优先级（可放 Phase 3）

---

## 3 · Agent3 授信 (credit) · PARTIAL

- **红线闸门**：✅ 全绿（halluc=0 / evidence=1.0 / task_completion=1.0）
- **实算条数**：6 / 10
- **Gap top 3**：
  1. `field_completeness = 0.9260`（target 0.90）——刚过线，gap = +0.026；mock 数据已预填，Phase 2 真数据会更紧
  2. `redline_detection_accuracy = 0.9744`（target 0.95）——过线但贴着，mock cases 有 2-3 个 decision↔hit_red_lines 不一致的灰区
  3. `credit_limit_reasonability = 0.0`（target ≤ 0.20）——偏乐观：mock 中批准 case 的 requested == approved，没有额度调整，实战会比这数字差

**改进建议**：
- `tool_success_rate` = pending —— 需 `agent_credit/api.py` 埋 tool_calls 元数据到响应里
- `ratio_calc_consistency` —— 等 code-urgent §3.1 `financial_analyzer` 接入 Agent3 后可算（现状 mock cases 里 debt_ratio 是预填静态值，无 runtime 比率运算）
- `score_human_agreement` / `terminology_compliance` —— Phase 2 需业务方提供真值 / 术语表

---

## 4 · Agent4 预警 (alert) · PASS 🟢

- **红线闸门**：✅ 全绿
- **实算条数**：6 / 10
- **Gap top 3**：
  1. `signal_diversity = 0.0`（target ≥ 0.60）—— fixture `phase0_scan_sample.json` 里客户无 `trigger_reasons` 字段填充，adapter 算出 0；yaml 已 pending 豁免 verdict
  2. `cross_hit_precision` / `recall_on_known_bad` = pending（Phase 2 Batch 2 已知问题客户真值库未到位）
  3. `field_completeness` = pending（B1 新加，adapter 未埋点）

**改进建议**：
- **最快收益**：让 `MockSearchProvider` + `AlertKnowledgeBase.from_scenario` 的 fixture 填充 `trigger_reasons`，signal_diversity 直接可算（adapter 逻辑已在）
- **field_completeness**：agent4 adapter 补 4 键完整率探针
- **cross_hit / recall**：等业务方提供已知 known-bad 清单

---

## 5 · Agent5 合规 (compliance) · PARTIAL

- **红线闸门**：🟡 全 N/A（无 runtime dump）
- **实算条数**：0 / 10
- **Gap top 3**：无实算数字可比，全量缺口——
  1. 缺 `agent_compliance.policy_scanner` runtime dump（抽取条款 + 冲突点 + 证据链）
  2. 缺业务方提供的 gold 条款集 / gold 冲突清单
  3. 缺监管术语表 v1.0（terminology_compliance）

**改进建议**：
- **Phase 2 先做 runtime dump** — 在 `agent_compliance.api.py` scan endpoint 里埋点把 conflict_items + extracted_clauses 写 `evaluation/manual/5_latest.json`，先解 field_completeness / task_completion / evidence_rate / evidence_completeness 4 条
- **policy_coverage / conflict_recall**：业务方提供政策条款 gold 集（可从既有合规检查历史扒）
- **defect_classification_accuracy**：gold_severity_map 从历史审核记录反向提取

---

## 6 · Agent6 报告 (report) · FAIL 🔴

- **红线闸门**：🟡 部分 N/A（halluc / evidence / field_completeness 都 pending · task_completion PASS）
- **实算条数**：3 / 10
- **Gap top 3**：
  1. `template_leakage_rate = 1.0`（target ≤ 0.02）—— **artifact 退化问题**：本轮传入 `samples/普惠申报书_骨架型.docx` 本身就是 baseline，自对比必 100% 泄漏，**非真实 gap**
  2. `unfilled_marker_accuracy = 0.75`（target ≥ 0.95）—— 骨架中含 `XXX` / `【】` 占位符本就是预期，同样**非真实 gap**
  3. `financial_ratio_consistency` / `section_length_calibration` / `quality_score_total` = pending（Phase B 需 `financial_analyzer` + 真人范文库 + `quality_scorer` runtime 接入）

**改进建议**：
- **Phase 2 最紧迫**：跑 `py v16_pipeline.py --source samples/普惠申报书_骨架型.docx --material samples` 产真 v16 输出 docx，入 `outputs/` 做真基线
- 真 v16 产出 docx 的 baseline `template_leakage_rate` 应落在 2026-04-15 附近的历史基线 `field_completeness=0.935` 同量级
- `quality_score_total` 直接复用 `quality_scorer.py`，adapter 补 1 行 import + 调用

---

## 下一步行动清单

| 优先级 | 改动 | 归属 | 预计 |
|---|---|---|---|
| 🔴 高 | 跑真 v16 产出 docx，重跑 agent6 基线 | evaluation worker / Phase B | 0.5 天 |
| 🔴 高 | 补 agent1 / agent5 api.py runtime dump 埋点 | code-urgent / code-arch | 1 天/Agent |
| 🟡 中 | 补 agent2 / agent4 adapter 新加指标的探针 | evaluation worker Phase 2 | 0.5 天 |
| 🟡 中 | 业务方提供 gold 条款集 / 真值库 / 术语表 | PM + 业务 | 外部依赖 |
| 🟢 低 | LLM-judge 打 portrait_match_precision / rule_interpretability | Phase 3 | — |

---

## 方法论备忘

- **首轮偏乐观是设计使然**：CLAUDE.md §5.2 "baseline-first" 原则——先建 rubric + 跑基线看 ceiling，再改代码找 floor。本轮 verdict 分布（PASS 1 / PARTIAL 4 / FAIL 1）合理反映了 data-foundation 依赖尚未解锁的现状。
- **Pending 不是 FAIL**：A-013 kernel 通过 `baseline.pending_metrics` 白名单从 verdict 分母移除，避免"不可算"污染判定。
- **Blocker threshold 尚未启用**：新 schema 的 `blocker_threshold` 本轮仅作为"发布闸门参照线"，尚未接入 CI/发布流程。Phase 2 接入。
