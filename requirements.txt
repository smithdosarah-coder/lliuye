# =============================================================================
# 信贷 AI 智能体矩阵 · 运行时依赖
# 真实版本锁定（DoD L0-8 要求 ==）由 Phase 1 各 Agent 用 `pip freeze > requirements.lock`
# 在自己 worktree 内生成。本文件仅声明工程范围，与 pyproject.toml 同步。
# =============================================================================

# Web 框架
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9

# HTTP 客户端
httpx>=0.27

# LLM
openai>=1.50

# 数据处理
pandas>=2.2
numpy>=1.26

# 文档处理
python-docx>=1.1
python-pptx>=1.0
openpyxl>=3.1
xlsxwriter>=3.2
lxml>=5.0

# 金融数据
akshare>=1.15

# 旧版 UI（Agent6 单机版 app.py 仍依赖；Phase 2 后可移除）
gradio>=4.44
