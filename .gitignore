# ----------------------------------------------------------------------------
# Python
# ----------------------------------------------------------------------------
__pycache__/
*.py[cod]
.venv-tmp/
*$py.class
*.so
.Python
*.egg-info/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
build/
dist/
wheels/
.venv/
venv/
env/
ENV/

# ----------------------------------------------------------------------------
# Secrets / env (CRITICAL — never commit)
# ----------------------------------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key
secrets/
credentials.json

# ----------------------------------------------------------------------------
# Caches (Tavily / akshare / 自建 KB cache 不进 git)
# ----------------------------------------------------------------------------
.cache/
*.cache
*_cache/

# ----------------------------------------------------------------------------
# Outputs / generated artifacts
# ----------------------------------------------------------------------------
out_*/
output/
outputs/
_chunks/
_screenshots/
*.docx.tmp
~$*.docx
~$*.xlsx
*.log
logs/
fill_run_output.txt
data/audit/*.jsonl
data/audit/*.db
data/audit/*.db-journal
data/feedback/*.jsonl
!data/feedback/.gitkeep
!data/audit/.gitkeep
# B4-riskctrl Sprint 3 BE8.6 PSI 月度跑产物 (jsonl per month · runtime 数据 · 不入库)
# Owner: B4-riskctrl · Justification (codex review V2 major 2):
#   PSI jsonl 是 agent_riskctrl.psi_monthly.write_psi_jsonl 输出 · BE8.6 owns ·
#   与 data/audit/*.jsonl + data/feedback/*.jsonl 同性质 (runtime data · gitkeep 保结构)
data/riskctrl/psi/*.jsonl
!data/riskctrl/psi/.gitkeep
!data/riskctrl/psi/README.md
data/exports/
data/alert/sessions/
data/alert/latest.json
!data/alert/.gitkeep
data/compliance/sessions/
data/compliance/latest.json
!data/compliance/.gitkeep
data/im/*.db
data/im/*.db-*
!data/im/.gitkeep
data/ledger/*.sqlite
data/ledger/*.sqlite-journal
data/ledger/*.sqlite-wal
data/ledger/*.sqlite-shm
data/ledger/*.db
data/ledger/*.db-journal
!data/ledger/.gitkeep
data/policy_registry/*.sqlite
data/policy_registry/*.sqlite-journal
data/policy_registry/*.sqlite-wal
data/policy_registry/*.sqlite-shm
data/policy_registry/*.db
data/policy_registry/*.db-journal
!data/policy_registry/.gitkeep
data/monitoring/
evaluation/results/*.yaml
evaluation/results/**/*.json
!evaluation/results/.gitkeep
evaluation/runtime/2_*/
evaluation/manual/2_*_run_*.yaml

# ----------------------------------------------------------------------------
# UI debug artifacts (playwright traces / 根目录 PNG 探索)
# ----------------------------------------------------------------------------
.playwright-mcp/
/*.png

# ----------------------------------------------------------------------------
# IDE / OS
# ----------------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db
desktop.ini

# ----------------------------------------------------------------------------
# Claude Code local artifacts
# ----------------------------------------------------------------------------
.claude/scheduled_tasks.lock
.claude/settings.local.json
.claude/worktrees/

# ----------------------------------------------------------------------------
# Multi-CLI mesh：每 worktree 本地身份文件（resume 用，不入库）
# ----------------------------------------------------------------------------
AGENT_IDENTITY.md

# ----------------------------------------------------------------------------
# Phase B.1/B.2 · agent handoff 真产物 (api 写盘 · 不入 git)
# ----------------------------------------------------------------------------
data/handoff/report_to_credit/*.json
!data/handoff/report_to_credit/.gitkeep
data/handoff/channel_to_credit/*/
data/handoff/channel_to_credit/*.json

# ----------------------------------------------------------------------------
# Orchestrator runtime artifacts (auto-generated, per-host)
# ----------------------------------------------------------------------------
recovery/
docs/handoff/scoreboard.md
docs/handoff/mesh-status.json
docs/handoff/watchdog-events.jsonl
docs/handoff/.watchdog-state.json

# ----------------------------------------------------------------------------
# Frontend (web/ Next.js)
# ----------------------------------------------------------------------------
web/.next/
web/node_modules/
web/.env.local
web/.env.*.local
web/_screenshots/
web/next_dev*.log
web/*.log

# ----------------------------------------------------------------------------
# Customer / sensitive data (绝对禁止)
# ----------------------------------------------------------------------------
customer/*.docx
customer/*.xlsx
customer/*.pdf
!customer/.gitkeep
!customer/README.md

# ----------------------------------------------------------------------------
# Stage 5 · 设计 CLI 过程截图不入库 (9.2MB 探索态，易污染 worker 视觉基线)
# ----------------------------------------------------------------------------
design_mockups/stage5/original/uploads/

# ----------------------------------------------------------------------------
# Playwright admin auth storage state (NEVER commit · contains admin localStorage)
# ----------------------------------------------------------------------------
web/tests/e2e/.auth/

# ----------------------------------------------------------------------------
# W1-mock-test · tests/ harness (npm install 产物 + 跑测 cache · 不入库)
# Owner: W1-mock-test worker · Stage: W1 准备周 · 见 docs/handoff/W1-mock-test-progress.md
# 第 5 棒决策: tests/package.json + tests/package-lock.json 入库 (Playwright 1.60 + ajv +
# tsx + jsdom 全量 lock · 跨 CI / 跨 dev 可重现 npm ci) · 但 node_modules + 跑测产物不入库.
# ----------------------------------------------------------------------------
tests/node_modules/
tests/coverage/
tests/.vitest-cache/
tests/test-results/
tests/playwright-report/
tests/.playwright/
