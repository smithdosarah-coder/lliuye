# 信贷 AI 智能体矩阵 · Demo 启动说明

> 众安信科 · 乾策平台 X-Nexus / Credit Intelligence Matrix v1.0
> 最后更新：2026-04-13

---

## 一、这个 Demo 演示什么

6 个 AI Agent 覆盖信贷全流程，都挂在一个 Portal 里，客户点 Tab 就能切：

| Tab | Agent | 场景 |
|-----|-------|------|
| 📄 报告生成 | CreditReportAgent | 读材料 → 生成 1.5 万字授信调查报告（独立 app.py 启动）|
| 🎯 全渠道流量匹配 | ChannelMatchAgent | 行业/地区/企业关键词 → 匹配资金需求线索 |
| 💼 授信决策辅助 | CreditDecisionAgent | 读企业材料 → 四维风险评估 + 额度建议 |
| ⚙️ 风控策略运营 | RiskControlAgent | 规则配置 / 回测分析 / 差错案件 |
| 🚨 贷中风险预警（精品）| AlertMonitorAgent | 红/黄/绿信号灯 Dashboard + 处置建议 |
| ✅ 合规巡检 | ComplianceAgent | 政策文件 + 业务记录 → 合规缺陷分级 |

**核心演示 Tab**：🚨 贷中风险预警 — 信号灯脉冲动画 + 三栏仪表盘，客户第一视觉焦点。

---

## 二、环境准备

### 依赖

项目根目录已自带 Python 3.14 + 以下库：

```
gradio >= 6.9
openai >= 2.29
pydantic >= 2.12
python-docx
pdfplumber
openpyxl
```

如果要从零安装：

```bash
pip install gradio openai pydantic python-docx pdfplumber openpyxl
```

### API Key

支持 5 个厂商（在 `config.py` 配置），任选一个：

| Provider | 用途 | 获取 |
|----------|------|------|
| `deepseek`（默认）| DeepSeek V3 | https://platform.deepseek.com |
| `kimi` | Moonshot Kimi K2.5 | https://platform.moonshot.cn |
| `minimax` | MiniMax Text-01 | https://platform.minimaxi.com |
| `openai` | GPT-4.1 | https://platform.openai.com |
| `anthropic` | Claude Opus | https://console.anthropic.com |

**不需要写进文件**。启动 Portal 后在顶部"⚙️ 全局配置"抽屉里填一次，6 个 Tab 共享。

---

## 三、启动方式

### 方式 1：Portal（推荐，看整套演示）

```bash
cd "D:/claude code/credit_report_agent_work"
python portal_app.py
```

浏览器打开 **http://localhost:7870**。

### 方式 2：单独启动某个 Agent（调试用）

```bash
python agent_alert/app_demo.py      # 贷中预警独立调试
python app.py                        # 报告生成助手独立调试（端口 7860）
```

---

## 四、演示流程（客户坐在会议室的 5 分钟）

### 第 1 分钟：Portal 首页
- 打开 http://localhost:7870
- 展示众安蓝品牌条 + 6 Tab 导航
- 展开顶部"全局配置"抽屉，输入 API Key → 保存

### 第 2 分钟：🚨 贷中风险预警（门面）
- 点"🚨 贷中风险预警" Tab
- 顶部看到 3 张场景卡：🔴 华联精密（红灯）/ 🟡 盛达贸易（黄灯）/ 🟢 顺通物流（绿灯）
- 点"🔴 华联精密" → 企业名和文件自动填入
- 点"开始预警分析"
- **关键视觉瞬间**：
  - 右上信号灯从灰色 → 蓝色转圈 → **🔴 红色+脉冲动画**
  - 下方三栏同时填充：风险信号表格（6+ 条）/ 趋势分析 / 处置建议卡片
  - 底部可展开"AI 分析过程"看实时思考流
- 点"📥 下载预警报告" → 导出 Markdown

### 第 3 分钟：💼 授信决策辅助
- 切到"💼 授信决策辅助" Tab
- 点"制造业授信评估"场景卡
- 上传预置的企业材料 → 点"开始评估"
- 展示：四维风险评估 + 建议额度 + 审批意见

### 第 4 分钟：🎯 全渠道流量匹配 + ⚙️ 风控策略运营
- 切 Tab → 快速点场景卡 → 展示匹配结果 / 回测报告

### 第 5 分钟：✅ 合规巡检 + 📄 报告生成
- 合规巡检：点"商业银行互联网贷款办法"场景 → 展示合规缺陷清单
- 报告生成：说明独立启动方式（`python app.py`），产出完整 1.5 万字报告

---

## 五、预置场景清单

项目启动时无需上传任何文件，点场景卡一键演示：

### 🚨 贷中风险预警（3 个）
- `demo_data/agent_alert/huaLian_red.json` — 华联精密机械 红灯
  - 营收 ↓18%、利润转负、涉诉 1200 万、客户重整、5 条负面舆情
- `demo_data/agent_alert/shengDa_yellow.json` — 盛达贸易 黄灯
  - 法人变更、经营范围跨界、杠杆升至 67%、存货周转恶化
- `demo_data/agent_alert/normal_green.json` — 顺通物流 绿灯
  - 稳健经营对照样本

### 🎯 全渠道流量匹配（2 个）
- `manufacturing_hunt.json` — 福建精密制造中小企业获客
- `tech_hunt.json` — 深圳 SaaS 创业公司获客

### 💼 授信决策辅助（2 个）
- `manufacturing_eval.json` — 某制造有限公司授信评估
- `trade_eval.json` — 某贸易公司授信评估

### ⚙️ 风控策略运营（2 个）
- `strategy_backtest.json` — 小微信用贷策略回测
- `bad_case_analysis.json` — 差错案件分析

### ✅ 合规巡检（2 个）
- `bank_loan_policy.json` — 商业银行互联网贷款管理办法
- `kyc_check.json` — KYC 合规性检查

---

## 六、文件地图

```
credit_report_agent_work/
├── portal_app.py                    # 统一入口（主演示用）
├── app.py                           # 报告生成独立版
├── shared/
│   ├── demo_ui.py                   # 主题色/CSS/场景加载/事件流 helper
│   ├── base_agent.py                # Agent 基类
│   └── base_app.py                  # Gradio 通用组件
├── agent_alert/                     # 贷中预警（精品 Demo）
│   ├── agent.py                     # 业务后端
│   ├── app_demo.py                  # 精品 UI（信号灯 + 三栏）
│   └── alert_engine.py / trend_analyzer.py / disposition.py
├── agent_channel/ agent_credit/ agent_riskctrl/ agent_compliance/
│   └── 各含 agent.py + app_demo.py + 业务模块
├── demo_data/                       # 预置场景
│   └── agent_alert/ agent_channel/ ... 各 Agent 子目录
├── docs/                            # PRD 文档（7 份）
│   ├── 改造方案_信贷AI智能体矩阵_v1.0.md
│   ├── PRD_客户经理个人助手_v3.0.md
│   └── PRD_<各 Agent>_v1.0.md
└── outputs/                         # 运行时产出（报告 / 导出）
```

---

## 七、常见问题

**Q1：启动报 `LLMClient got unexpected keyword 'model_provider'`**
这个 bug 之前修过了。如果再出现，检查 `shared/base_agent.py` 第 30 行是否为 `self.llm = LLMClient(provider=model_provider, api_key=api_key)`。

**Q2：点场景卡后输入框没填充**
场景 JSON 缺字段。检查对应文件至少有 `scenario_id` / `title` / `input_message` 三个字段。

**Q3：贷中预警跑完但信号灯还是灰色**
多半是 API Key 没填。展开顶部"⚙️ 全局配置"保存 Key。

**Q4：端口 7870 被占**
在 `portal_app.py` 最底部改 `server_port=7870` 为其他端口。

**Q5：我要改演示场景数据**
直接改 `demo_data/<agent_name>/*.json` 或同目录下的 `.txt` mock 数据文件，改完刷新浏览器即可。

---

## 八、已知限制（下一迭代处理）

- 跨 Agent 数据流（EnterpriseProfile）尚未接通 — 目前每个 Agent 独立运行
- 报告生成 Tab 只是跳转提示，未嵌入 Portal
- 导出功能只有贷中预警实现（Markdown），其他 4 个 Agent 没做导出
- Plotly 真折线图（趋势分析）未上，用 Unicode bar 代替
- 没有登录/鉴权（Demo 期不需要）

---

## 九、联系

- 项目维护：刘野（众安信科 · 乾策平台 X-Nexus）
- PRD / 改造方案：见 `docs/` 目录，已同步至飞书 wiki
