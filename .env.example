# =============================================================================
# 信贷 AI 智能体矩阵 · 环境变量模板
# 复制为 .env 后填实际值。.env 文件已在 .gitignore 中，禁止 commit。
# =============================================================================

# ---------------------------------------------------------------------------
# LLM Provider（必需）
# DeepSeek 为默认境内 LLM。客户材料处理仅允许走境内 API（合规要求）
# ---------------------------------------------------------------------------
DEEPSEEK_API_KEY=sk-your-deepseek-key-here

# 可选：兼容性别名，部分模块读 LLM_API_KEY 做 fallback
# LLM_API_KEY=

# ---------------------------------------------------------------------------
# 外网搜索 / 政策抓取（必需 for Agent1/4/5 真实模式；mock 模式可省）
# Tavily 为获客 / 预警 / 合规 共享的搜索通道
# ---------------------------------------------------------------------------
TAVILY_API_KEY=tvly-your-tavily-key-here

# ---------------------------------------------------------------------------
# 网络代理（可选）
# 国内访问 OpenAI / Tavily 偶发不稳定时配置
# ---------------------------------------------------------------------------
# HTTPS_PROXY=http://127.0.0.1:7890
# HTTP_PROXY=http://127.0.0.1:7890

# ---------------------------------------------------------------------------
# FastAPI 服务端口（可选，默认 8000）
# 与前端 web/.env.local 的 NEXT_PUBLIC_API_BASE 对齐
# ---------------------------------------------------------------------------
PORT=8000

# ---------------------------------------------------------------------------
# Agent6 报告 — 并发控制（可选）
# 调高加快但会撞 LLM rate limit；保守 6/10 足够
# ---------------------------------------------------------------------------
NARRATIVE_WORKERS=6
SECTION_GEN_WORKERS=10

# ---------------------------------------------------------------------------
# Agent1 获客 — Web 搜索 LLM 选择（可选）
# 默认 deepseek；实验性可切其他境内大模型
# ---------------------------------------------------------------------------
CHANNEL_WEB_LLM_PROVIDER=deepseek

# ---------------------------------------------------------------------------
# Agent1/4/5 — 知识库扫描 demo 模式（可选）
# true: 强制 mock 数据，断网可演示；false: 必须真 Tavily
# ---------------------------------------------------------------------------
KB_SCAN_DEMO_MODE=true
